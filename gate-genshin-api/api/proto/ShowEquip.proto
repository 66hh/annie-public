syntax = "proto3";

package proto;
option go_package = "./;proto";

import "Reliquary.proto";
import "Weapon.proto";

message ShowEquip {
  oneof detail {
    Reliquary reliquary = 2;
    Weapon weapon = 3;
  }
  uint32 item_id = 1;
}
