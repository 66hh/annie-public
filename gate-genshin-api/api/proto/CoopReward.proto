syntax = "proto3";

package proto;
option go_package = "./;proto";


message CoopReward {
  enum State {
    STATE_UNLOCK = 0;
    STATE_LOCK = 1;
    STATE_TAKEN = 2;
  }

  uint32 id = 1;
  State state = 2;
}
