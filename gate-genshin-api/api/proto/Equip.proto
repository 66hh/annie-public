syntax = "proto3";

package proto;
option go_package = "./;proto";

import "Reliquary.proto";
import "Weapon.proto";

message Equip {
  oneof Detail {
    Reliquary reliquary = 1;
    Weapon weapon = 2;
  }
  bool is_locked = 3;
}
